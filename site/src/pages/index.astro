---
import Layout from "../layouts/Layout.astro";
import Home from "../components/Home.astro";
---
<Layout lang="en" title="Solon — Docker on Windows without Docker Desktop or WSL" description="Solon is a standalone, open-source container manager for Windows: a complete Docker engine in a tiny Linux machine it manages itself. 4× less memory than Docker Desktop, local HTTPS addresses for every container, no telemetry.">
  <Home lang="en" />
</Layout>
