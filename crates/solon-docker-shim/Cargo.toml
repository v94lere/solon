[package]
name = "solon-docker-shim"
description = "docker.exe de Solon : lance le CLI Docker officiel en le dirigeant vers le moteur Solon"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "docker"
path = "src/main.rs"

[dependencies]
serde_json = { workspace = true }
